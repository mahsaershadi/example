<h1>Dashboard</h1>
<a href="/posts/new">Create New Post</a>
{{#each posts}}
  <div class="post">
    <h2>{{this.title}}</h2>
    <p>{{this.content}}</p>
    <small>By {{this.author.username}}</small>
  </div>
{{/each}}